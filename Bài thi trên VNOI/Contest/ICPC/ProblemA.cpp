#include <bits/stdc++.h>
#define ll long long
#define frt(i, a, b) for(int i=a; i<=b; i++)
#define frg(i, a, b) for(int i=a; i>=b; i--)

using namespace std;
const int N = 1e2 + 2;
int n, m, t;
int tree[N][N][N];

void built(int id, int l, int r, int id_x, int id_y, int id_z, int val)
{

}

int main()
{
    cin.tie(nullptr)->sync_with_stdio(false);
    cin >> t;
    while(t--){
        cin >> n >> m;
        frt(i,1,m){
            int x, y, z;
            cin >> x >> y >> z;

        }
    }
}
